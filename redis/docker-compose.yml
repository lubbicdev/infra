version: '3.9'

services:
  redis:
    image: redis:7.2
    container_name: redis_server
    restart: always
    command: >
      redis-server --requirepass ${REDIS_PASSWORD}
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks:
      - infra

volumes:
  redis_data:

networks:
  infra:
    external: true
